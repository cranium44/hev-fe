import React from 'react';
import {default as Carousel} from '../../UIElements/carousel/Carousel';

const Home = pops => {
    return(
        <div>
            <Carousel/>
        </div>
    )
}

export default Home;